const toggle=document.querySelector('.toggle'),links=document.querySelector('.links');toggle&&toggle.addEventListener('click',()=>links.classList.toggle('active'));links&&links.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>links.classList.remove('active')));document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',function(e){const href=this.getAttribute('href');if(!href||href==='#')return;e.preventDefault();const target=document.querySelector(href);target&&window.scrollTo({top:target.offsetTop-80,behavior:'smooth'})}));const form=document.getElementById('contactForm'),status=document.querySelector('.status');form&&form.addEventListener('submit',e=>{e.preventDefault();const btn=form.querySelector('button');btn.textContent='Sending...';btn.disabled=true;setTimeout(()=>{status.textContent='Thank you! We\'ll respond soon.';status.classList.add('show','success');form.reset();btn.textContent='Send';btn.disabled=false;setTimeout(()=>status.classList.remove('show'),5000)},1000)});const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.style.opacity='1';entry.target.style.transform='translateY(0)'}})},{threshold:0.1});document.querySelectorAll('.product,.class-item').forEach((el,i)=>{el.style.opacity='0';el.style.transform='translateY(30px)';el.style.transition=`all 0.6s ease ${i*0.1}s`;observer.observe(el)});console.log('📖 Paper Craft & Bookbinding');
